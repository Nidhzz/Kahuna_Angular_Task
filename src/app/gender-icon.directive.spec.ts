import { GenderIconDirective } from './gender-icon.directive';

describe('GenderIconDirective', () => {
  it('should create an instance', () => {
    const directive = new GenderIconDirective();
    expect(directive).toBeTruthy();
  });
});
